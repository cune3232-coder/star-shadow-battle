# 📱 星影モバイル最適化プラン & 進捗状況

このドキュメントは、PC版ブラウザゲーム『星影』をスマートフォン（iPhone）で快適にプレイ可能にするための改修計画と、現在の進捗を記録するものです。

---

## ✅ Phase 1: 基礎 UX & 必須機能のスマホ対応 (完了)
基本的なゲームプレイをスマホで破綻なく行えるようにするための改修。

### 1. ログ・通知システム
- [x] **ログの格納**: 常時表示をやめ、トグルボタンで開閉するモーダルへ移行。
- [x] **テロップ表示**: 最新のログ1行を画面上部に常時表示。
- [x] **トースト通知**: 重要イベント（正体判明、撃破など）を画面中央に一時表示。

### 2. ターゲット選択の確実性
- [x] **全画面オーバーレイ**: 対象選択時に専用画面を表示し、誤タップを防止。
- [x] **グリッド表示**: プレイヤーパネルを大きく表示し、HPやチーム情報を確認しやすく。

### 3. 操作エリアの最適化
- [x] **ActionControlsの固定**: スマホ時は画面最下部に固定（ボトムシート風）。
- [x] **手札エリア調整**: 自分ターン以外は手札を縮小・透過させ、画面を広く使う。
- [x] **排他制御**: アクションボタンと手札が重ならないよう余白を調整。

### 4. カード操作 (Web標準化)
- [x] **長押し廃止**: iOSシステムメニューとの競合を回避。
- [x] **タップ挙動**: シングルタップで選択（ポップアップ）、選択状態で使用。
- [x] **Click Feedback**: タップ時のアニメーション（沈み込み）を追加。

---

## 🚧 Phase 2: UX向上 & 詳細な調整 (未着手)
ストレスフリーな操作感を目指すフェーズ。

### 1. 視覚的洗練
- [ ] **Safe Area対応**: iPhoneのノッチ（上部）やホームバー（下部）に要素が被らないよう `pb-safe`, `pt-safe` 等を適用。
- [ ] **対戦相手リスト**: スマホ表示時、よりコンパクトな専用デザイン（アバターアイコンのみ等）への切り替え検討。

### 2. フィードバック強化
- [ ] **Haptics (触覚)**: カード使用時やダメージ発生時に `navigator.vibrate` で振動フィードバックを追加（Android/一部ブラウザ）。
- [ ] **Animation**: ターゲット選択オーバーレイの出現アニメーションや、カードドロー時の演出強化。

### 3. その他
- [ ] **PWA化**: ホーム画面に追加した際、アドレスバーを消してフルスクリーンアプリとして動作させる (`manifest.json`)。
- [ ] **横持ち対応**: ランドスケープモード時のレイアウト崩れ防止（現状は縦持ち推奨）。

---

## 📝 開発メモ
- **コンポーネント**: `LogViewer`, `TargetSelectionOverlay` はスマホ専用（またはレスポンシブ）として実装済み。
- **CSS**: Tailwind の `md:` プレフィックスを使用して PC/スマホ のスタイルを切り替えている。
